import React, { useState } from "react";
import SkillChip from "./SkillChip.jsx";
export default function SkillInput({skills,setSkills}){ const [text,setText]=useState(''); function add(){ const name=text.trim(); if(!name) return; if(skills.some(s=>s.name.toLowerCase()===name.toLowerCase())){ setText(''); return;} setSkills([...skills,{name,level:6}]); setText(''); } function update(i,level){ const next=[...skills]; next[i]={...next[i],level}; setSkills(next);} function remove(i){ setSkills(skills.filter((_,idx)=>idx!==i)); } return (<div className='glass p-4 rounded-xl'><h2 className='text-lg font-semibold mb-2'>Skills</h2><div className='flex gap-2'><input value={text} onChange={e=>setText(e.target.value)} className='w-full p-2 glass rounded bg-transparent' placeholder='Add skill e.g. React' onKeyDown={e=>e.key==='Enter'&&add()}/><button onClick={add} className='px-4 py-2 bg-purple-600 rounded'>Add</button></div><div className='mt-4 flex flex-wrap gap-3'>{skills.map((s,i)=>(<SkillChip key={s.name+i} skill={s} onRemove={()=>remove(i)} onLevelChange={(lvl)=>update(i,lvl)}/>))}</div></div>); }
