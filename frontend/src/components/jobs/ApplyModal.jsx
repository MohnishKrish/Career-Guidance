import React,{useState} from "react";
import toast from "react-hot-toast";
export default function ApplyModal({job,onClose}){ const [form,setForm]=useState({name:'',email:'',message:''}); const [file,setFile]=useState(null);
  async function submit(e){ e.preventDefault(); if(!form.name||!form.email||!file){ toast.error('Please complete required fields'); return;} toast.success('Application submitted'); onClose(); }
  return (<div className='fixed inset-0 flex items-center justify-center z-50'><div className='absolute inset-0 bg-black/60' onClick={onClose}></div><form onSubmit={submit} className='relative z-10 glass p-6 rounded-lg w-full max-w-lg'><h3 className='text-lg font-semibold mb-3'>Apply: {job.title}</h3><input className='w-full p-2 glass rounded mb-2' placeholder='Full name' value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/><input className='w-full p-2 glass rounded mb-2' placeholder='Email' value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/><input type='file' accept='.pdf' onChange={e=>setFile(e.target.files?.[0])} className='mb-2'/><textarea className='w-full p-2 glass rounded mb-3' rows='4' placeholder='Message (optional)' value={form.message} onChange={e=>setForm({...form,message:e.target.value})}/><div className='flex justify-end gap-2'><button type='button' onClick={onClose} className='px-3 py-1 rounded border'>Cancel</button><button type='submit' className='px-4 py-1 rounded bg-purple-600'>Send</button></div></form></div>); }
