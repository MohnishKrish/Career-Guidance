import { useState } from "react";
import api from "../../api/api.js";
import { useNavigate } from "react-router-dom";
export default function SignupForm(){ const nav=useNavigate(); const [form,setForm]=useState({email:'',password:''}); const submit=async()=>{ try{ const res=await api.post('/auth/signup',form); if(!res.data.error) nav('/'); }catch(err){ alert(err.response?.data?.error||'Signup failed'); } }; return (<div className='glass p-6 rounded-xl w-80'><h2 className='text-xl font-bold mb-4'>Sign Up</h2><input type='email' placeholder='Email' className='w-full p-2 mb-3 glass rounded' onChange={e=>setForm({...form,email:e.target.value})}/><input type='password' placeholder='Password' className='w-full p-2 mb-3 glass rounded' onChange={e=>setForm({...form,password:e.target.value})}/><button onClick={submit} className='w-full p-2 bg-purple-600 rounded'>Sign Up</button><p className='mt-3 text-sm'>Have an account? <a href='/' className='text-purple-300'>Login</a></p></div>); }
